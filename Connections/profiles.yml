Cocacola_DWH:
  outputs:
    dev:
      dbname: coke_db # postgres (currently)
      host: localhost
      pass: postgres
      port: 5432
      schema: public
      threads: 4
      type: postgres
      user: postgres
  target: dev

